<h1>Kitten Details</h1>
<table border>
  <thead>
  <tr>
    <th>Name</th>
    <th>Age</th>
    <th>Cuteness</th>
    <th>Softness</th>
  </tr>
  </thead>
  <tbody>
  <% if @kitten %>
    <tr>
      <td><%= @kitten.name %></td>
      <td><%= @kitten.age %></td>
      <td><%= @kitten.cuteness %></td>
      <td><%= @kitten.softness %></td>
    </tr>

    </tbody>
    </table>
    <%= link_to "Delete", kitten_path(id: @kitten.id), method: :delete, data: { confirm: "Are you sure you want to delete kitten?" } %>
  <% end %>
  <%= link_to "Back", root_path %>